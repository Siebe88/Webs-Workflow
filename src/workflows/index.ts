import * as simpleWorkflow from './simpleWorkflow';

//TODO this needs to be constructed better so that only the workflows are attached
const workflows: any = {};

workflows.simpleWorkflow = simpleWorkflow;

export default workflows;
